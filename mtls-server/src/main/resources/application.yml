server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:${KEYSTORE:keystore.jks}
    key-store-password: changeit
    key-alias: localhost
    key-password: changeit

    client-auth: need
    trust-store: classpath:${TRUSTSTORE:server-truststore.p12}
    trust-store-password: changeit
